services:
  - type: web
    name: monitoria-planetun
    runtime: python
    buildCommand: pip install -r requirements.txt && python init_db.py && python seed_data.py
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
